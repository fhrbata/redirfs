		================================
		RedirFS - Redirecting FileSystem
			      TODO 
		================================

* support for sub-mounts
* support for character devices, block devices, FIFOs, symbolic links and sockets
* allow filters to attach their private data to the VFS objects
* allow filters to hand over data from pre to post callback function
* port to 2.4 Linux kernel
