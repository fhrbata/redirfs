* readdir() - not RedirFS f_ops
* finish d_revalidate callback
	- d_invalidate returns -EBUSY because there
	are still refences in path
* correct locking and references
	- deadlocks appear with same paths in branches
* more than one mountpoint support
 	=> correct handling with root private data
* polish the source
